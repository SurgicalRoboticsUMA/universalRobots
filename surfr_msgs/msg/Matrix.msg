# The multiarray declares a generic multi-dimensional array of a
# particular data type. User is free to order data as desired. The
# only requirement is that dimension of data array must be the same
# size as the multiplication of elements within dim array.

Header header
std_msgs/MultiArrayLayout layout
float64[] data
